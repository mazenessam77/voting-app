##############################################################################
# secret.yaml — Sensitive configuration (JWT signing key)
#
# IMPORTANT: Replace the base64 value before deploying.
#   echo -n "your-production-secret" | base64
##############################################################################
apiVersion: v1
kind: Secret
metadata:
  name: voting-app-secret
  namespace: voting-app
type: Opaque
data:
  # Base64 of "super-secret-key" — CHANGE THIS for production
  JWT_SECRET: c3VwZXItc2VjcmV0LWtleQ==
